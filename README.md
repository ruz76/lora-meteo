# lora
Simple meteo data visualisation. The data are distributed by lora network.

Install:

* Run npm install to get node.js modules
* Change in main.js serviceUrl: "http://localhost/lora/get.php"
* Run npm run serve

This should lead into web available at http://localhost:8080/

Build for production:
* Run npm run build
